;;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((nil
  (projectile-project-test-cmd . "yarn run coverage")
  (projectile-project-configure-cmd . "yarn run clean && yarn run cleandeps && yarn install")
  (projectile-project-compilation-cmd . "yarn run compile")
  )
 )
