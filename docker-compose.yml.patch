diff --git a/dist/ci/docker-compose.yml b/dist/ci/docker-compose.yml
index cb319226..1f0b302b 100644
--- a/dist/ci/docker-compose.yml
+++ b/dist/ci/docker-compose.yml
@@ -17,6 +17,8 @@ services:
     command: /usr/sbin/memcached -u memcached
   api:
     <<: *obs
+    ports:
+      - "8080:3000"
     command: >
       chroot --userspec=wwwrun / /bin/bash -c "cd /srv/www/obs/api &&
          RAILS_ENV=production rake ts:rebuild &&
@@ -40,15 +42,3 @@ services:
   smtp:
     <<: *testenv
     command: python3 /code/dist/ci/smtp/eml-server.py
-  test:
-    <<: *testenv
-    depends_on:
-      - api
-      - smtp
-    command: /code/dist/ci/docker-compose-test.sh
-    cap_add:
-      - SYS_PTRACE
-  flaker:
-    <<: *testenv
-    command: /code/dist/ci/docker-compose-flaker.sh
-
